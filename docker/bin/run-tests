#!/usr/bin/env bash

set -ex

# launch an instance within AWS
./bin/instance-launch

# install Docker, Docker Compose, and Bash Completions
./bin/docker-setup

# setup tests
./bin/dtest-setup

# run tests
./bin/dtest-run

# destroy ec2 machine
./bin/instance-destroy
